server:
  port: 8098

spring:
  application:
    name: spring-test

  redis:
    database: 1
    host: 127.0.0.1
    port: 6379
    password:
    jedis:
      pool:
        max-active: 20
        max-wait: -1
        max-idle: 8
        min-idle: 0
    timeout: 1000


  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin